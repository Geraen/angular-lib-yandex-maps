import { PlacemarkDirective } from './placemark.directive';

describe('PlacemarkDirective', () => {
  it('should create an instance', () => {
    const directive = new PlacemarkDirective();
    expect(directive).toBeTruthy();
  });
});
